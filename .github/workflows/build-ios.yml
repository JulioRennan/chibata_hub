name: Reusable • Build iOS

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true

jobs:
  build:
    runs-on: macos-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: Build iOS
        run: echo "Build iOS "